import React from 'react';

const Substract = (props) => {
    return (
        <div>
            <h1>import substarct function</h1>
            <h4>{props.result}</h4>
        </div>
    );
};

export default Substract;